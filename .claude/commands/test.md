---
description: Run backend tests using test-backend.sh and frontend tests using test-frontend.sh in parallel.
---

Run `./test-backend.sh` and `./test-frontend.sh` in parallel and report the results from both test suites.
