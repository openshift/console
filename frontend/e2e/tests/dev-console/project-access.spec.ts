import { test } from '../../fixtures';

test.describe('Customize Project Access roles', { tag: ['@regression', '@dev-console'] }, () => {
  test('adding custom role in project membership', async () => {
    test.skip(true, 'Manual test — requires YAML editing of cluster console resource');
  });

  test('removing custom role in project membership', async () => {
    test.skip(true, 'Manual test — requires YAML editing of cluster console resource');
  });
});
