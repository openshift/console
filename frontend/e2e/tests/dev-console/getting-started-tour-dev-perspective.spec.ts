import { test } from '../../fixtures';

test.describe(
  'Getting Started tour of developer perspective',
  { tag: ['@regression', '@dev-console'] },
  () => {
    test('quick tour when user logs in for the first time', async () => {
      test.skip(true, 'Manual test — guided tour disabled in Cypress due to flakes');
    });

    test('quick tour from help menu', async () => {
      test.skip(true, 'Manual test — guided tour disabled in Cypress due to flakes');
    });

    test('stopping quick tour in mid of the tour', async () => {
      test.skip(true, 'Manual test — guided tour disabled in Cypress due to flakes');
    });
  },
);
