import { test } from '../../fixtures';

test.describe('Login to developer perspective', { tag: ['@regression', '@dev-console'] }, () => {
  test('developer perspective display on login with developer credentials', async () => {
    test.skip(true, 'Marked @to-do in Cypress — requires separate developer credentials');
  });
});
