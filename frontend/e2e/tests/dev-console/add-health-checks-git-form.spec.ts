import { test } from '../../fixtures';

test.describe('Health Checks in git import form', { tag: ['@smoke', '@dev-console'] }, () => {
  test('health checks option in advanced options', async () => {
    test.skip(true, 'Marked @to-do in Cypress — covered by A-04-TC12');
  });
});
