import * as React from 'react';
import { observer, Node } from '@console/topology';

const ParallelSpacerNode: React.FC<{ element: Node }> = () => {
  return <g />;
};

export default observer(ParallelSpacerNode);
