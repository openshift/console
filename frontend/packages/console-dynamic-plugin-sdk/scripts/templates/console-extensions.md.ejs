# OpenShift Console Extension Types

<%# Table of contents _%>
<% extensions.map((e) => e.type).forEach((type, index) => { _%>
<%- index + 1 %>. [`<%- type %>`](#<%- type %>)
<% }); _%>

<%# Generate one section per extension type _%>
<% extensions.forEach((e) => { _%>
---

## `<%- e.type %>`

### Summary

<%- printComments(e.docComments) || '(not available)' %>

### Properties

| Name | Value Type | Description |
| ---- | ---------- | ----------- |
<% e.properties.forEach((p) => { _%>
| `<%- escapeTableCell(p.name) %>` | `<%- escapeTableCell(p.valueType) %>` | <%- escapeTableCell(printComments(p.docComments)) %> |
<% }); _%>

<% }); _%>
