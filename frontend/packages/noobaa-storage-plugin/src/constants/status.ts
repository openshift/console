export enum StatusType {
  HEALTH = 'HEALTH',
  RESILIENCY = 'RESILIENCY',
}

export enum Phase {
  CONNECTED = 'Connected',
  PROGRESSING = 'Progressing',
  FAILURE = 'Failure',
  READY = 'Ready',
}
