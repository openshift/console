export enum VMWizardName {
  YAML = 'yaml',
  WIZARD = 'wizard',
  BASIC = 'basic',
}

export enum VMWizardMode {
  IMPORT = 'import',
  TEMPLATE = 'template',
  VM = 'vm',
}

export enum VMWizardView {
  ADVANCED = 'advanced',
  SIMPLE = 'simple',
}
