apiVersion: v1
kind: Service
metadata:
  namespace: coreupdate
  name: coreupdate
spec:
  selector:
    coreupdate-component: app
  type: NodePort
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    nodePort: 30002
