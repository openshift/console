apiVersion: v1
kind: Service
metadata:
  namespace: coreupdate
  name: coreupdate
spec:
  selector:
    coreupdate-component: app
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
