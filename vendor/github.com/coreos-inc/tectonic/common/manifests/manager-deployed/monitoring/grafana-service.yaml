apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    app: grafana
    area: system
spec:
  type: NodePort
  ports:
  - port: 3000
    protocol: TCP
    nodePort: 32004
  selector:
    app: grafana
    area: system

