apiVersion: v1
kind: Service
metadata:
  name: tectonic-id-overlord
  labels:
    app: tectonic-identity
    component: overlord
spec:
  selector:
    app: tectonic-identity
    component: overlord
  type: ClusterIP
  ports:
  - name: overlord
    protocol: TCP
    port: 5557
