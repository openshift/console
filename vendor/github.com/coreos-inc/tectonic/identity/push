#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

set -ex
TECTONIC_VERSION=$("${DIR}/../git-version")

"${DIR}/../builder-run" make -f "identity/Makefile" docker-image-deps

docker build -t quay.io/coreos/tectonic-identity:$TECTONIC_VERSION $DIR/.workspace/tmpdir
docker tag quay.io/coreos/tectonic-identity:$TECTONIC_VERSION quay.io/coreos/tectonic-identity:vdevel-latest

docker push quay.io/coreos/tectonic-identity:$TECTONIC_VERSION
docker push quay.io/coreos/tectonic-identity:vdevel-latest

