#!/bin/bash -e

export PROJECTS="soy creme soyctl"
export PROJECT_REPO=github.com/coreos-inc/soy
export IMAGE_REGISTRY=quay.io
export IMAGE_REPO=quay.io/coreosinc/soy
export IMAGE_REPO_BASE=quay.io/coreosinc

export BUILDER_REPO=coreosinc/soy-builder
export GO15VENDOREXPERIMENT=1
export BUILDER_TAG=v1.3
export DOCKER_RUN_ARGS=${DOCKER_RUN_ARGS:-}
