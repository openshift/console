#!/bin/bash -e

source build-env

if [ -z "$PROJ" ]; then
    BUILDABLE_PROJECTS="${PROJECTS}"
else
    BUILDABLE_PROJECTS=$PROJ
fi

for p in ${BUILDABLE_PROJECTS}; do
  echo "Building ${p}..."
  CGO_ENABLED=0 go build -o bin/${p} $PROJECT_REPO/cmd/${p}
done
